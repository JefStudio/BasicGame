# Auto detect text files and perform LF normalization
* .h5 filter=lfs diff=lfs merge=lfs -text=auto
* git add .gitattributes
* git add model.h5
* git commit -m "add file with trained neural network"
* git push origin master
